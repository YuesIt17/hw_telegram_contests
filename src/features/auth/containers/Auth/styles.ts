import {css} from '@emotion/react';

export const useStyles = () => ({
  input: css({
    border: '1px solid rgba(0, 0, 0, 0.23)',
    borderRadius: 4,
    padding: '14px 12px',
    outline: 0,
  }),
});
